/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package org.gradlex.plugins.analyzer.app;

import org.gradlex.plugins.analyzer.list.LinkedList;

import static org.gradlex.plugins.analyzer.utilities.StringUtils.join;
import static org.gradlex.plugins.analyzer.utilities.StringUtils.split;
import static org.gradlex.plugins.analyzer.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
